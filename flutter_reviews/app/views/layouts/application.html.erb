<!DOCTYPE html>
<html class="min-h-screen bg-transparent">
  <head>
    <title><%= content_for(:title) || "FlutterReviews" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Island Formulator">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>
    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen flex flex-col bg-purple-300">

    <!-- HEADER -->
    <header class="mt-2 bg-black shadow-lg w-full">
      <div class="mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-18">

          <!-- Left side -->
          <div class="flex-shrink-0 flex items-center gap-4">
            <%= link_to "FlutterReviews🦋", root_path,
                class: "text-4xl font-bold text-white py-2" %>

            
              <%= link_to "Search Books 🔎", search_path, class: "text-gray-500 hover:text-green-600 transition-colors font-semibold mt-4" %>
              <%= link_to "Search by Author 🕵️‍♂️", books_by_index_path, class: "text-gray-500 hover:text-yellow-300 transition-colors font-semibold mt-4" %>
          </div>

          <!-- Right side -->
          <nav class="hidden md:flex space-x-6">
            <div class="flex items-center gap-4">
              <% if authenticated? %>
                <span class="text-sm text-gray-500">
                  <%= Current.user.email_address %>
                </span>

                <%= button_to "Logout", session_path, method: :delete,
                    class: "px-4 py-2 bg-gray-700 text-gray-300 hover:text-red-500 font-medium rounded-md transition-colors" %>
                 
                 <%= button_to "View Profile", user_path(Current.user), method: :get,
                    class: "px-4 py-2 bg-purple-700 text-gray-100 hover:text-purple-200 font-medium rounded-md transition-colors" %>


              <% else %>
                <div class="flex space-x-2">
                  <%= link_to "Sign Up", new_user_path,
                      class: "px-4 py-2 text-sm font-medium text-white bg-blue-600 hover:bg-blue-100 rounded-md transition-colors" %>

                  <%= link_to "Login", new_session_path,
                      class: "px-4 py-2 text-sm font-medium text-white bg-gray-700 hover:bg-gray-100 rounded-md transition-colors" %>
                </div>
              <% end %>
            </div>
          </nav>

        </div>
      </div>
    </header>

    <!-- MAIN CONTENT -->
    <main class="flex-1 bg-transparent w-full">
      <%= yield %>
    </main>

  </body>
</html>